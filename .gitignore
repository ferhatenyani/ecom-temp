.env
.env.*
!.env.example

*.log

wp-content/uploads/

vendor/
node_modules/

.phpunit.cache/
.phpunit.result.cache/

.vscode/
.idea/

docker-data/
.claude/settings.local.json

# Per-client configuration (roadmap §71, §73). Holds no secrets — secrets are
# .env only — but it is one client's details, and this is the template.
client.json
